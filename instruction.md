A failed backup-platform rollout truncated the authoritative snapshot inventory at `/app/data/snapshots.json` and left the control-plane retention reconciler at `/app/workflow/reconcile_retention.py` evaluating stale draft rules instead of the storage governance board's final decisions. Nothing the reconciler produces can be trusted until that inventory is rebuilt. A pre-incident catalogue at `/app/data/snapshot_catalogue_pre_incident.json` and a replay journal of the records created after it at `/app/data/snapshot_replay_journal.json` survive beside the truncated file. How the two merge, which wins where they overlap, how a retraction is handled and the order of the result are governance decisions, recorded like every other one in `/app/incident/retention_governance_log.md`, and the inventory must be restored first, in place at `/app/data/snapshots.json` where the truncated one sits.

Then restore the reconciler, preserving its `--input` and `--output-dir` options and their defaults; the retention policy and the pin registry are always read from their fixed absolute paths under `/app/data`, and `--input` selects the inventory only. `/app/docs/report_spec.json` is the output contract: paths, schemas, required-field lists, field coercions, container shapes and sort orders. It says nothing about how any value is derived. Reconstruct that from `/app/incident/retention_governance_log.md`, mostly routine noise, where the later dated decision governs. Each snapshot also holds a protection window, and the board wants both its overlap with its repo's other windows and how deeply those windows stack inside it reported per snapshot, as `overlap_count` and `peak_depth` on every decision row; on repos holding tens of thousands of snapshots a run must still finish inside the contract's 120-second budget. A run writes exactly `/app/output/summary.json`, `/app/output/retention_state.json` and `/app/output/retention_decisions.jsonl`. Derive every value from the operational inputs, which are read rather than rewritten: the pre-incident catalogue, the replay journal, the retention policy, the pin registry and `/app/docs/report_spec.json` come back byte-identical. The inventory is the one thing under `/app/data` that gets replaced, and that replacement belongs to the first step and happens once: the reconciler itself writes nothing under `/app/data` at all. It reads the inventory, the registers and the policy from their fixed paths and puts everything it produces in its output directory. The reconciler runs without permission to write under `/app/data`, so one that folds the recovery into its own run and rewrites the inventory dies there instead of reconciling anything. Correct on any snapshot stream that conforms to the contract rather than on the graded one alone, identical across reruns, and leave the frozen incident snapshot `/app/workflow/.reconcile_retention.original` exactly as it is.
